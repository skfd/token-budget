

## Rules

- Don't change or add files outside project directory.
- Don't try to add individual file ItemGroup in `csproj` -- project includes them automatically.
- Don't create `nul` file. Delete one if you see one.